# 상품순환파일 만들기 자동화

## 이미지 처리 프로세싱
현재 '이미지필터링' 경로의 모든파일을 순환파일로 만듬(단. xls 파일 처리위주로 만들어짐)
해당파일의 '목록이미지' 열의 행을 리스트로 만들어 이미지필터링(이미지에 문자가 있는지)후
문자있음, 문자없음 을 구별 정렬하여 output.xls 파일을 생성 (순환에 쓰일파일)
이후 '필터링url모음파일.xlsx'를 만들어 필터링 내용을 저장
문자있음, 문자없음 탭을 만들어 각각 저장
파일이 만들어진 이후에는 이미지 필터링전 모음파일의 내용이 없는것만 필터링을 하게됨


## 설치 
https://github.com/UB-Mannheim/tesseract/wiki 설치

python -m venv myenv
myenv\Scripts\activate

pip install pytesseract Pillow requests
pip install pandas openpyxl
pip install openpyxl
pip install xlrd
pip install xlrd openpyxl
pip install xlwt


Tesseract 경로, 엑셀 경로 및 파일 확장자는 config/settings.py 파일에서 변경할 수 있습니다.
OCR 관련 로직은 ocr/ocr_checker.py에서 수정 가능합니다.
엑셀 관련 로직(열 변경, 저장 경로 변경 등)은 excel/excel_handler.py에서 수정 가능합니다.
로그 출력을 변경하거나, 다른 형태로 확장하려면 utils/url_checker.py에서 수정 가능합니다.

- 순환자동화
현재 로그인이후 엑셀다운로드에서 막힘. 
일단 다운로드까지 기다리고 폴더에 .crdownload 파일이 생성되면
브라우저의 다운로드 아이콘을 클릭후 계속 다운로드로 수동으로 다운로드 되도록 설정하자 

## 패키지 설치 
pip install selenium
pip install webdriver-manager


## 크롬드라이버 최신버전 설치 
from webdriver_manager.chrome import ChromeDriverManager
driver_path = ChromeDriverManager().install()
print(f"path : {driver_path}")
path : C:\Users\Administrator\.wdm\drivers\chromedriver\win64\130.0.6723.58\chromedriver-win32/chromedriver.exe


## 진행상황
- 10/20
F:\work\#쇼핑몰\#대량등록\#상품순환 엑셀파일\이미지필터링 으로 파일경로를 정하고 
파일경로에 있는 파일을 기준으로 변경하였음.

- 10/21
엑셀파일 다운로드가 제대로 되지않음
파일경로를 설정하여도 파일경로가 먹히지 않음. (된적도 있었기 때문에 한글문제는 아닌듯한데 확인필요)

- 10/23
이미지필터링
Google Cloud Vision API 로 사용하기로 결정
일단은 이번에 만들어진 순환파일들로 테스트 해보고 되겠다 싶으면 (현재 테스트중)

필터링된 url을 저장하는 모음url엑셀파일을 만들었음

할일 10/24 >> 
1. 모음url엑셀파일이 있는경우 없는것만 필터링, 중복처리 - 완료
2. 글자있음 정렬 해결하기 - 행전체가 정렬되어야 함 -완료

할일 10 /25 >>
1. 링크모음 파일과 비교해서 이전에 문자있음 이력이 있는지 판별해 이력이 있는경우 목록있음 
으로 표시하여 이미지필터에 들어가지 않는 것으로 변경됨
2. 단지 글자없음의 경우 이력이 남지 않아 계속 필터링 될수 밖에 없으므로 이걸 어떻게 해결할것인가
문제는 output 파일이 있어야 되고(이미지 있음과 없음의) 그에 맞는 링크들도 있어야 되기때문에 
해결이 어렵다. 그래서 일단 output파일을 변경하기로 함
기존 url열을 활용하지 않고 새로운 열을 만들어 그곳에 이내용들을 정리
그렇게 하면 순환도 가능하고 이력도 남길수 있을듯..
- 우선 output파일의 변경 하기 
내일 다시 생각해보고 하나씩 적용해보자. 


- 진행상황 10/27
현재 '이미지필터링' 경로의 모든파일을 순환파일로 만듬(단. xls 파일 처리위주로 만들어짐)
해당파일의 '목록이미지' 열의 행을 리스트로 만들어 이미지필터링(이미지에 문자가 있는지)후
문자있음, 문자없음 을 구별 정렬하여 output.xls 파일을 생성 (순환에 쓰일파일)
이후 '필터링url모음파일.xlsx'를 만들어 필터링 내용을 저장
문자있음, 문자없음 탭을 만들어 각각 저장
파일이 만들어진 이후에는 이미지 필터링전 모음파일의 내용이 없는것만 필터링을 하게됨

버전 0.1로 릴리즈(나중에 돌아오기 위한 태그)
git tag -a v0.1 -m "버전 0.1 릴리즈"
git push origin v0.1
* 이후 이버전으로 돌아오는것은 그때가서 다시 확인해보자. 

##  이후 할일 
1. 새로운 버전으로 순환파일을 적용해 순환해보고 오류 확인
2. 로그인 이후 파일다운로드 픽스
3. 다운로드 받은 파일 순환파일 만들기
5. 최초 로그인전 번호등을 입력해 해당 순환파일 모두를 다운받아 처리하게 만들기 

* 위의 내용들은 이후 상품명가공과 카테고리작업이후 속행예정

# 상품명가공 자동화 시작 (10/29)

## 진행상황
상품명 가공 및 복사: 상품명을 읽고 새 열에 배치하여 데이터를 재구성.
색상 적용 기능: 특정 조건에 따라 색상을 지정해 상품의 구분을 용이하게 처리.
정렬 기능 분리 및 개선: 정렬과 색상 적용을 상황에 맞게 분리해, 필요한 기능을 유연하게 호출할 수 있도록 개선.
출력 파일 생성: 모든 작업을 수행한 후 output 파일로 저장하여, 최종 결과를 검토할 수 있게 구조화.

### 10/30
네이버api 테스트 완료

### 10/31
네이버api 테스트완료 : 현재 네이버쇼핑+ 는 지원되지 않고 비교상품 쪽으로 검색결과가 반환됨

### 10/31
gpt연동완료 : 기본상품명에서 메인상품명을 가져올수 있게 되었음

- gpt연동
pip install openai==0.27.0


## 가공목표 할일
1. 네이버 API 설정 및 상위 키워드 수집하여 정제하는것을 목표로 만들것 
2. 네이버 API로 기본 최적화를 진행하면서도 구매 고객을 중심으로 한 추가 분석, 
   상세페이지와의 연관성 확보, 트렌드 반영을 결합하는 방식으로 진행해보자. 
3. 위의 내용 이전에 기본상품명에서 핵심 키워드를 빼내는 작업부터 진행 


### 기본상품명에서 핵심키워드 빼내기 - 완료
기본상품명을 키워드로 각각 분류해서 사전과 대조
메인키워드가 있으면 메인키워드와 그에 속한 보조키워드를 반환
만약 메인키워드가 없다면 gpt를 이용해 메인키워드를 찾음
여러개를 넣어 테스트 해보자. 

### 기본상품명에서 키워드를 추출한 이후 작업
상품가공로직을 참고로 이후 작업을 계속해 나가자. 
일단 gpt 불러오는 부분부터 다시 코드를 보며 테스트를 계속해보자. 아직 gpt부분이 보강되어야 할듯함 
특히 중복되는 키워드의 부분 쉼표로 나열하는 것으로 바꾸고 이후 꺼낼때에도 이부분이 반영되도록 바꾸어야함. 

pip install XlsxWriter


현재 메인, 보조, 패턴 을 결합해 상품명을 만들게 되었음.
그런데 중복키워드의 경우 어떻게 할지 생각해봐야됨. 
만약 메인키워드가 반복된다면 메인과 보조키워드를 쉼표없이 하나의 키워드로 만드는 것도 가능할듯. 
이부분은 다시 생각해보자. 
중요 : 1. 일단 변경되었음
사전에 있는 경우에도 GPT를 부르는 경우에도 고정키워드(기본상품명의 메인키워드 이외의 키워드) 가
필요하여 이부분을 구현함. 키워드인포등 변경되었으니 이후 나머지 부분들도 맞추어서 변경해야됨 

### 상품명 가공 1차완료 11/16
현재 키워드 사전을 만들어 
기본상품명의 메인키워드가 있으면 
메인키워드와 보조키워드를 반환하고 없으면 gpt에게 물어 
메인과 보조키워드를 나누고 보조키워드는 사양,용도 등으로 분별하여
사전에 저장.

이렇게 하여 추출된 메인키워드를 네이버검색에 넣어 
상위 10개의 상품명을 가져와서 그중 자주 언급된 키워드 위주로 패턴을 만들었음
그리고 기본상품명에서는 메인키워드 이외의 키워드는 고정키워드로 만들어
꼭 상품명에 들어가게 하였음
이렇게 하여 메인키워드 + 고정키워드 + 패턴 을 조합하여 가공상품명을 만듬
이후 이렇게 만들어진 상품명의 내부를 랜덤으로 바꾸어 차별화를 두게 하였음

만들어진 최종 가공상품명을 간단한 필터와 상품명 리스트의 중복방지를 통해 seo를 확립함

이후 테스트 해보고 상품명의 요소들을 유사키워드등으로 바꿀지는 아직 미정

경쟁이 적은 키워드(니치 키워드) 를 활용하여 
상위판매 키워드와 니치키워드를 혼합하는 방식을 추구

### 테스트 필요
현재 상위키워드 만을 가지고 패턴을 만들었지만 
하위 키워드를 가지고 니치시장을 노릴수도 있으니 테스트 해보자. 

### 이후 버전
패턴에서 만들어진 키워드 일부를 
네이버 연관검색어 api를 이용하여 유사키워드로 변경하여 만들어보는 방법 

### 상품명 가공을 깃허브에 업데이트 
git add .
git commit -m "상품명 가공 프로토타입 구현 및 판매자코드 업데이트 기능 추가"
git tag -a v0.2 -m "버전 0.2 릴리즈: 상품명 가공 프로토타입 추가"

### 11/18
- 메인키워드 안나오는 오류 수정
- 잘못 만들어진 'filtered' 타입 삭제함"
git tag -a v0.3 -m "버전 0.3 릴리즈: 메인키워드 공란 및 판매차접두사 오류잡음"

### 할일
1. 지피티 내용 안나왔을때 예외처리
2. 네이버검색 내용 안나왔을때 예외처리
3. 네이버검색을 많이 안부르게 하려면???

### 상품명 가공 피벗!!!!!
현재 상위판매자의 키워드 방식은 브랜드명을 처리하기가 어려운 단점이 있음
그래서 이부분은 브랜드키워드 사전이 만들어진 이후 다시 사용하기로 함
그래서 최초는 연관검색어를 활용해 패턴을 만드는 것으로 변경
GPT요청시에도 메인+보조키워드 추출 및 추천 연관검색어와 추천 브랜드명을 추가로 받음
연관검색어는 메인키워드에 매칭시켜 추가해 나가고 ..

일단 연관검색어등은 잘 도출됨
추가로 도출형식을 제대로 확립하고 사전에도 연관검색어,브랜드키워드 열을 만들어 테스트 
처음부터 하나하나 다시 만들어 나가자 

네이버 연관검색을 위해 메인키워드는 최소단위로 뽑아야 한다.
그래서 프롬프트에 최소단위로 뽑을수 있도록 추가문구를 넣자.
이후엔 연관검색으로 뽑아진 데이터를 어떻게 활용할지 생각해보자. 


1. gpt 호출도 기본상품명이 중복되면 부르지 않는걸로 바꾸자. #11/24 완료
2. 네이버 api호출 최소화 하기 
3. 네이버연관검색에도 브랜드명이 있으니
연관검색으로 뽑인 단어들을 네이버검색을 다시돌려 상위 10개의 상품의 
브랜드명을 브랜드키워드에 추가하는 형식으로 해보자.
4. 상위판매자 검색은 결과값에 '브랜드명'이 있었으므로 이것을 빼내는 것으로 활용할수 있을듯 

### 12/1 상품명가공 프로토타입 최종완료
- 기본상품명에서 메인키워드와 고정키워드를 추출하여 네이버 광고 API를 활용한 연관검색어 검색 구현
- 연관검색어 기반 패턴 생성 및 가공상품명 생성 로직 추가
- API 호출 중복 방지를 위한 사전저장 및 조건부 패스 로직 최적화
- 최종 필터링 및 엑셀 데이터 업데이트 작업 완료
- 주요 오류(메인키워드 공란 및 판매자코드 접두사 문제) 수정 완료
git tag -a v0.4 -m "버전 0.4 릴리즈: 네이버 연관검색어 기반 상품명 생성 로직 및 API 최적화"

### 앞으로 할일
- 직접 테스트
- 필터링 추가
- 연관검색 이외의 가공추가
- 서버에 올리기 (이미지 필터링, 상품명가공)

### 12/15
git tag -a v0.5 -m "버전 0.5 릴리즈: 네이버 연관검색 제거 및 GPT 검색결과 기반 상품명 생성"


### 순환파일 자동화 12/18 시작
1. 이셀러스 카테고리중 식품관련 카테고리 정리 - 완
2. 카테고리 필터링 - 완
3. 이미지 필터링 편집중
 - 필터링 이후 바로 삭제 버전, 색상버전 만들자 - 완료
 - 완료후 패키지 정리 - 완료
 - 데이터 저장을 json으로 변경 - 완
 -- 데이터형식을 정하고 테스트 - 완
 -- 엑셀로부터 데이터 이관 - 완
 -- 이미지필터링시간, 비용 계산  - 일단완료(나중에 수정필요 : 출력부분이 약간 불안정 )

4. 상품순환 자동화  
   1. < 카테고리 번호* >열 비어있는 행삭제 - 테스트중 , 이미지필터링 삭제작업에 무결성 코드 넣기 - 완
   2. < 상품명* >열 비어있는 행삭제 - 완
   3. < 카테고리 번호* >열이 음식카테고리인 경우 행삭제- 완
   4. < 상품명* >열이 중복된 경우 행삭제- 완
   5. < 선택사항 타입 >열 비어있지 않은경우 제거- 완
   6. < 검색어(태그) >열 의 문자열에서(예:귀후비개,귀파개,led,라이트,손전등) 중복 키워드 제거, 숫자 및 특수문자 제거, 문자열을 29바이트 내외로 조정
   ---------  이셀러스에 태그 몇자까지 혹은 몇byte까지 가능한지 확인하기----------
   7. < 요약정보 상품군 코드* > 35로 모두변경- 완
   8. < 요약정보 전항목 상세설명 참조 > "Y" 로 모두변경- 완
   9. 상품시트 행분할 -완
   10. 각 순환파일 리포트 작성

### 상품순환 자동화 완료 2025-01-12
git tag -a v0.4 -m "버전 1.0 릴리즈: 2025-01-12 순환자동화 완료"

## 상품가공 할일
코드정리하기 (순환파일 함수들 정리) - 완 
도매토피아 변환 2행 내용제거하기 - 완
상품명중복 - 상품명 없는것도 지우기 
validate 적용하여 코드 정리하기 



가공이후 가공의 성공, 실패 로그 보여주기
엑셀 읽는작업 이미지필터링 내용으로 변경하기 (.xlsx 로 저장등등 )

0. 중복 GPT 출력 테스트, 비용계산표시 -> 완료 
0-1. 진행율 표시
1. gpt에게 연관검색어 더 뽑아내서 테스트해보기 - 좀더 다양한 연관검색어를 얻기 위해 어떻게 해야할지 생각해보자. 
2.  gpt에게 복합명사 없애도록 프롬프트 추가 
3. 특수문자 (/_ 없애기 , 4자이상 숫자 없애기
4. 연관검색에 숫자등 사양관련 안나오게 하 기
5. 키워드에서 특수문자 제거후 남은 키워드는 각각 분리해서 각각의 보조키워드로 만들자 
6. 영문(사양) 대체 문구 만들기 
7. 가공이 짧은 문자열은 사전에서 지우는 코드를 만들자. 
- 고정키워드도 같이 지워야 됨 
8.  '. +' 등 사양 관련해서 필터링에서 배제 
- 그리고 마지막 상품명에서 특수문자 제거 추가 
9 gpt 호출횟수 확인 -로그 참고 
0.5mm 중성볼펜
0-9 원형 숫자 번호 스티커 1장
1.5m 다이어트줄자
1.5m 원형 열쇠고리 줄자











